/**
 * ProductPage component
 * 
 * This is a new product page that can be customized independently from ListingPage.
 * Initially, it uses ListingPageCarousel as a base, but can be modified later
 * without affecting the original ListingPage.
 * 
 * For now, we simply re-export ListingPageCarousel, but this file can be customized
 * later to create a completely different product page design.
 */
export { default } from '../ListingPage/ListingPageCarousel';

